# Internal design doc (kept out of git intentionally)
WC_Fantasy_Handover.md

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Runtime artifacts (regenerated by `update`; user-specific)
data/snapshots/
data/reports/
data/state.json
data/events.log
# local web data (the committed docs/data.json is CI-owned)
docs/data.local.json

# Keep committed: data/fixtures/ (offline test data), config.yaml, priors.yaml
!data/fixtures/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/